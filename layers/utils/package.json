{
	"name": "@repo/utils",
	"type": "module",
	"version": "0.0.1",
	"main": "./nuxt.config.ts",
	"scripts": {
		"dev": "nuxi dev -p 3002",
		"build": "nuxt build",
		"generate": "nuxt generate",
		"preview": "nuxt preview",
		"postinstall": "nuxt prepare",
		"lint": "biome lint ./ --write",
		"format": "biome format ./ --write",
		"update": "pnpx npm-check-updates -ui"
	},
	"devDependencies": {
		"@repo/biome-config": "workspace:*",
		"nuxt": "^4.1.3",
		"typescript": "^5.9.3"
	}
}
